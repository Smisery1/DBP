/**
 * Central information about the DataPuzzler project to be edited are managed from here.
 */

package dbp

case class ProjectSetup(path: String)

/**
 * Class managing program runs. Creating process objects and distributing tasks among them.
 */
object Manage {
	def apply(parser: CommandLineParser) = {
	
	  
	}
}