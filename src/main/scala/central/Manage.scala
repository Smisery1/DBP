package central

case class ProjectSetup(path: String)

/**
 * Central class. Creating process objects and distributing tasks among them.
 */
object Manage {
	def apply(parser: CommandLineParser) = {
	
	  
	}
}